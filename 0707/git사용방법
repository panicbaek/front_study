집에서 내려받을 때
1. 저장한 폴더를 하나 만든다.
2. 해당 폴더에서 git bash를 연다.
3. git clone 저장소주소 .
 위 명령어를 치면 폴더에 내려받아 진다.
4. 작업할거 있으면 그냥 하면됨
5. 다시 깃허브에 올리기 위해서는 
git add . / git commit -m '커밋메세지' gka
6. git push origin main 까지 해주면 깃허브에 올라감

=============충돌이 생일 수 있음 ===============
동일한 파일이 깃허브와 내 컴퓨터에 있는 부분이 겹쳐서
우리는 집에서 학원에서 매번 push와 pull을 해주면 생길일이 희박함
학원에서 집갈때 push하고 집에서 코드 건들기 전 pull해주고
집에서 다 할거 끝나면 add, commit, push
학원와서 뭐 하기전에 곡 pull해주고

만약 pull하면서 충돌나면 해결해주고 add, commit만 해주면 상관없긴함
근데 만약 집에서 충돌해결 못할거 같으면 새폴더 만들고 clone부터 다시하면 되긴함 